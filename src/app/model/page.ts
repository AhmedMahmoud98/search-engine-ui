/**
 * Result : It represents a website page  
 * that retrieve from the back end
 */
export interface Page {
    /** The page title */
    title: string;
    /** The page URL */
    url: string;
    /** The page summary */
    summary: string;
}
